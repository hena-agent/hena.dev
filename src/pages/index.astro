---
import { ProductPreview } from "@/components/ProductPreview"
import { WaitlistForm } from "@/components/WaitlistForm"
import { site } from "@/data/site"
import Layout from "@/layouts/main.astro"

const turnstileSiteKey = import.meta.env.PUBLIC_TURNSTILE_SITE_KEY ?? "1x00000000000000000000AA"

const providers = [
  "OpenAI",
  "Anthropic",
  "Google",
  "OpenRouter",
  "Bedrock",
  "Azure",
  "GitHub Copilot",
  "Ollama",
]
---

<Layout>
  <header class="site-header">
    <a class="wordmark" href="/" aria-label="Hena home">
      <span class="brand-mark" aria-hidden="true">H</span>
      <span>Hena</span>
    </a>
    <nav aria-label="Main navigation">
      <a href="#workspace">Workspace</a>
      <a href="#models">Models</a>
      <a href="#open-source">Open source</a>
      <a class="header-cta" href="#waitlist">Join waitlist</a>
    </nav>
  </header>

  <main id="main-content">
    <section class="hero" aria-labelledby="hero-title">
      <div class="hero-copy">
        <p class="launch-pill"><span></span> Desktop beta · {site.launch}</p>
        <h1 id="hero-title">One workspace for<br />chat, code, and<br /><em>autonomous work.</em></h1>
        <p class="hero-description">
          Hena brings every AI workflow into one local workspace, then routes each task to the
          model best suited to do it.
        </p>
        <div class="hero-actions">
          <a class="button button--primary" href="#waitlist">
            Join the waitlist <span aria-hidden="true">↗</span>
          </a>
          <a class="text-link" href="#workspace">See how it works <span aria-hidden="true">↓</span></a>
        </div>
        <p class="hero-note">Free and open source · Runs on your machine</p>
      </div>
      <div class="hero-index" aria-hidden="true">
        <span>01</span>
        <span>AI workspace</span>
      </div>
    </section>

    <section id="workspace" class="preview-section" aria-labelledby="preview-title">
      <div class="section-heading section-heading--preview">
        <div>
          <p class="kicker"><span>01</span> Projects shape the workspace</p>
          <h2 id="preview-title">The right mode<br />for every project.</h2>
        </div>
        <p>
          Create a project for conversation, a codebase, or autonomous web work. Each project keeps
          the sessions, context, and tools that fit its purpose.
        </p>
      </div>
      <div class="preview-label"><span>Interactive product preview</span><span>Switch projects ↓</span></div>
      <ProductPreview client:load />
    </section>

    <section class="modes-section" aria-labelledby="modes-title">
      <div class="section-heading">
        <div>
          <p class="kicker"><span>02</span> Purpose-built project modes</p>
          <h2 id="modes-title">Set the mode once.<br />Keep every session focused.</h2>
        </div>
      </div>
      <div class="mode-grid">
        <article class="mode-card mode-card--chat">
          <div class="mode-number">01 / Chat project</div>
          <div class="mode-visual" aria-hidden="true">
            <span class="chat-line chat-line--short"></span>
            <span class="chat-line"></span>
            <span class="chat-line chat-line--medium"></span>
          </div>
          <div>
            <h3>Think in conversation.</h3>
            <p>Keep research, decisions, and conversations together without code-specific tooling.</p>
          </div>
        </article>
        <article class="mode-card mode-card--code">
          <div class="mode-number">02 / Code project</div>
          <div class="mode-visual code-visual" aria-hidden="true">
            <span><i>+</i> const context = await workspace.load()</span>
            <span><i>+</i> return agent.run(task, context)</span>
          </div>
          <div>
            <h3>Work inside the codebase.</h3>
            <p>Give every session the repository, files, terminal, and review tools it needs to ship.</p>
          </div>
        </article>
        <article class="mode-card mode-card--automate">
          <div class="mode-number">03 / Claw project</div>
          <div class="mode-visual automate-visual" aria-hidden="true">
            <span class="browser-dot"></span>
            <span>open → inspect → act → report</span>
            <i></i>
          </div>
          <div>
            <h3>Hand off repetitive web work.</h3>
            <p>Keep browser tools, running tasks, and reviewable results inside an autonomous project.</p>
          </div>
        </article>
      </div>
    </section>

    <section id="models" class="routing-section" aria-labelledby="routing-title">
      <div class="routing-copy">
        <p class="kicker"><span>03</span> Model orchestration</p>
        <h2 id="routing-title">The right model<br />for the work at hand.</h2>
        <p>
          Hena routes for task quality by default. See what it picked and why, or lock any session
          to the model you choose.
        </p>
        <dl class="routing-facts">
          <div><dt>75+</dt><dd>model providers</dd></div>
          <div><dt>Local</dt><dd>credentials & sessions</dd></div>
          <div><dt>Any</dt><dd>OpenAI-compatible API</dd></div>
        </dl>
      </div>
      <div class="routing-demo" aria-label="Example model routing diagram">
        <div class="routing-request">
          <span>Incoming task</span>
          <strong>Implement and test<br />the new billing flow</strong>
        </div>
        <div class="routing-node">
          <span class="routing-node__signal"></span>
          <div><small>Hena router</small><strong>Optimize for quality</strong></div>
          <span class="routing-node__badge">Auto</span>
        </div>
        <div class="routing-options">
          <div><span class="model-glyph">A</span><p><strong>Claude Sonnet</strong><small>Selected · coding + tools</small></p><b>01</b></div>
          <div><span class="model-glyph">O</span><p><strong>GPT Codex</strong><small>Available · manual override</small></p><b>02</b></div>
          <div><span class="model-glyph">G</span><p><strong>Gemini Pro</strong><small>Available · long context</small></p><b>03</b></div>
        </div>
      </div>
    </section>

    <section class="providers" aria-label="Supported model providers">
      <p>Connect the models you already use</p>
      <div class="provider-list">
        {providers.map((provider) => <span>{provider}</span>)}
        <span>+ 67 more</span>
      </div>
    </section>

    <section id="open-source" class="open-source-section" aria-labelledby="open-title">
      <div class="open-source-art" aria-hidden="true">
        <div class="source-orbit source-orbit--one"></div>
        <div class="source-orbit source-orbit--two"></div>
        <div class="source-center"><span>H</span><small>MIT</small></div>
        <span class="source-label source-label--local">LOCAL FIRST</span>
        <span class="source-label source-label--extensible">EXTENSIBLE</span>
        <span class="source-label source-label--yours">YOUR WORKSPACE</span>
      </div>
      <div class="open-source-copy">
        <p class="kicker"><span>04</span> Open by design</p>
        <h2 id="open-title">Your agents.<br />Your models.<br />Your machine.</h2>
        <p>
          Hena is a free, MIT-licensed OpenCode fork. Inspect the system, extend the tools, and
          keep the workspace under your control.
        </p>
        <div class="local-boundary">
          <span class="status-dot"></span>
          <p><strong>Local means local.</strong><br />Execution, credentials, and sessions stay on your machine. Requests go directly to the provider you connect.</p>
        </div>
        <span class="source-coming">Source repository opening with the beta.</span>
      </div>
    </section>

    <section id="waitlist" class="waitlist-section" aria-labelledby="waitlist-title">
      <div class="waitlist-meta">
        <span>05 / Early access</span>
        <span>{site.launch}</span>
      </div>
      <div class="waitlist-content">
        <div>
          <p class="kicker kicker--dark"><span></span> Desktop beta</p>
          <h2 id="waitlist-title">Bring your AI work<br />into one place.</h2>
        </div>
        <div class="waitlist-action">
          <p>Join the waitlist for the first Hena desktop beta and open-source release.</p>
          <WaitlistForm
            client:load
            siteKey={turnstileSiteKey}
            consentVersion={site.consentVersion}
          />
        </div>
      </div>
    </section>
  </main>

  <footer class="site-footer">
    <a class="wordmark" href="/" aria-label="Hena home">
      <span class="brand-mark" aria-hidden="true">H</span>
      <span>Hena</span>
    </a>
    <p>Open-source AI workspace for chat, code, and autonomous work.</p>
    <div>
      <a href="/privacy">Privacy</a>
      <a href={`mailto:${site.email}`}>Contact</a>
      <span>© 2026 {site.company}</span>
    </div>
  </footer>
</Layout>
